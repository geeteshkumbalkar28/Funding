
#datasource
spring.datasource.url=jdbc:mysql://localhost:3306/FUNDING
spring.datasource.username=myuser
spring.datasource.password=MyP@ssword123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
jwt.secret=${JWT_SECRET:myTestSecretKey}
jwt.expiration=${JWT_EXPIRATION:86400}

# EMAIL - Alphaseam Mail Server Configuration
spring.mail.host=${MAIL_HOST:mail.alphaseam.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:testing@alphaseam.com}
spring.mail.password=${MAIL_PASSWORD:Alphaseam@!#8520}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# IMAP Configuration (for reference)
# imap.host=${IMAP_HOST:mail.alphaseam.com}
# imap.port=${IMAP_PORT:993}
# imap.auth=true

# POP3 Configuration (for reference)
# pop3.host=${POP3_HOST:mail.alphaseam.com}
# pop3.port=${POP3_PORT:995}
# pop3.auth=true

# RAZORPAY
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_a3kb1GXuvUpqcu}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:Xor0KhdlApRxLBS9EZLSgj1p}

# LOGGING
logging.level.com.donorbox=INFO
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO

# SWAGGER
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.swagger-ui.enabled=true

# UPLOAD - Updated for video support
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.file-size-threshold=2KB
app.upload.dir=uploads
app.base.url=http://localhost:8080

# MEDIA UPLOAD CONFIGURATION
# Image limits
app.upload.image.max-size=25MB
app.upload.image.allowed-types=jpg,jpeg,png,gif,webp,bmp,tiff,svg

# Video limits
app.upload.video.max-size=100MB
app.upload.video.allowed-types=mp4,avi,mov,wmv,flv,webm,mkv,m4v,3gp,ogv

# Document limits
app.upload.document.max-size=10MB
app.upload.document.allowed-types=jpg,jpeg,png,gif,webp,pdf,doc,docx

# SERVER
server.port=8003

# ADMIN EMAIL
admin.email=${ADMIN_EMAIL:testing@alphaseam.com}

# 👇 Active profile set to local by default
spring.profiles.active=local

# TIMEZONE CONFIGURATION
# Set default timezone to Asia/Kolkata for consistent date/time handling
spring.jackson.time-zone=Asia/Kolkata
